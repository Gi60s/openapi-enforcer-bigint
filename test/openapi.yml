openapi: '3.0.0'
info:
  title: ''
  version: ''
paths:
  /:
    get:
      responses:
        200:
          description: ''
          content:
            application/json:
              schema:
                type: string
                format: bigint
                minimum: '0'
                maximum: '10'